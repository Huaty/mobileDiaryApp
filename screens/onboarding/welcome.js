import {
  ScrollView,
  View,
  Text,
  StyleSheet,
  Dimensions,
  Image,
  SafeAreaView,
  TextInput,
  Button,
} from "react-native";
const { width } = Dimensions.get("screen");

const WelcomePage = () => {
  return (
    <View>
      <Text>Welcome To Diary!</Text>
    </View>
  );
};

style = StyleSheet.create({});

export default WelcomePage;
